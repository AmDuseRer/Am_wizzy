# 前后端技术学习 · 通用提问流程

> 面对任何新技术（Pydantic Schema、Vue、FastAPI、SQLAlchemy……），按下面 **3 个问题顺序** 向 AI 提问。  
> 目标：先懂「为什么需要」→ 再动手练 → 最后跑通代码，**不需要先学完所有前置知识**。

---

## 使用方式

1. 把 `{技术名称}` 换成你要学的东西（如 `Pydantic Schema`、`Vue 组合式 API`）
2. 可选：加上 `{本项目路径}`，让 AI 结合当前项目举例（如 `server/app/schemas/`）
3. 按 **问题一 → 问题二 → 问题三** 依次提问，不要跳步
4. 问题三得到的练习代码，建议放到 `xxx/` 或 `practice/` 目录自己跑

---

## 问题一：它到底有什么用？（建立动机）

### 提问模板

```
我现在刚开始学前后端，很多背景知识都不知道。

请仅就「{技术名称}」这个概念，结合{本项目}，
用最最通俗的方式（把我当成纯小白）回答下面三点：

1. 这个东西在项目中到底有什么不可或缺的作用？
2. 假如我不使用它，跟使用它，会有什么区别？
3. 带来的实质好处是什么？

要求：
- 不要堆砌术语和前置知识
- 每个好处最好配一个「不用 vs 用了」的对比
- 最后用 2～3 句话总结

```

### 你要从回答里拿到的

| 检查项 | 说明 |
|--------|------|
| 一句话定义 | 能否用生活类比讲清楚（如「登记表」「安检」） |
| 不用会怎样 | 具体痛点：重复代码、易出错、难维护…… |
| 用了会怎样 | 具体收益：自动检查、少写代码、前后端约定统一…… |
| 是否真「不可或缺」 | 不用能不能做？用了省多少事？ |

### 合格标准（自问）

- [ ] 我能用自己的话复述「这是干什么的」
- [ ] 我能说出一个「不用它会很烦」的场景
- [ ] 我还不需要懂周边技术，只懂这一件事即可

---

## 问题二：我能不能动手练？（巩固概念）

### 提问模板

```
我已经初步了解了「{技术名称}」。

请在仍然不要求我掌握相关前置知识的前提下，
给我设计 5 道由浅入深的动手练习题，让我手操巩固这个概念。

要求：
- 每道题说明「要练会什么」
- 给出测试数据或预期结果（什么该成功、什么该失败）
- 题目尽量自包含，不依赖完整项目环境（如只需 Python / 只需浏览器控制台）
- 最后一题可以是「迷你综合题」，最接近真实项目用法
- 练完后告诉我：应该打开项目里哪个文件对照看

我现在的水平：{纯小白 / 会一点 Python / 会一点 JavaScript …}
```

### 题目难度建议（给 AI 的隐含期望）

| 题号 | 难度 | 目标 |
|------|------|------|
| 1～2 | 入门 | 会定义、会跑通、会看到成功/失败 |
| 3～4 | 进阶 | 常见规则（必填、长度、可选字段） |
| 5～6 | 熟练 | 接近项目里的真实写法 |
| 7～8 | 综合 | 串起来模拟一个小流程 |

### 合格标准（自问）

- [ ] 我至少独立完成前 3 题
- [ ] 失败案例报错时，我知道是「规则生效了」而不是程序坏了
- [ ] 我能指出项目里对应的文件/代码段

---

## 问题三：给我一段能直接跑的代码（落地执行）

### 提问模板

```
请把上面第 {N} 题生成一段完整的、可执行的代码。

要求：
- 复制粘贴即可运行，包含必要的 import 和测试用例
- 运行方式写清楚（如 pip install xxx、node xxx.py）
- 预期输出大致长什么样
- 如果是 Windows 环境，避免控制台无法显示的字符（如 emoji）

在demo下建一个「{技术名称}」_demo目录，将代码脚本存入里面
```

### 常见输出格式

| 技术类型 | 建议文件 | 运行方式 |
|----------|----------|----------|
| Python 库（Pydantic 等） | `xxx/practice_xxx.py` | `python practice_xxx.py` |
| 前端（Vue / JS） | `xxx/practice_xxx.html` 或 `.js` | 浏览器打开或 `node` |
| SQL | `xxx/practice_xxx.sql` | 数据库客户端执行 |
| Shell / Git | `xxx/practice_xxx.sh` | 终端逐行试 |

### 合格标准（自问）

- [ ] 代码在我电脑上跑通了
- [ ] 成功和失败的 case 结果符合预期
- [ ] 我能改一个字段值，预测结果并验证

---

## 三步流程总览

```
┌─────────────────────────────────────────────────────────┐
│  问题一：为什么需要？（通俗、对比、好处）                  │
│           ↓  能用自己的话讲清楚                           │
│  问题二：怎么练？（5～8 题、自包含、对照项目文件）          │
│           ↓  至少做完前几题                               │
│  问题三：给我可执行代码（指定题号、指定目录、能跑）         │
│           ↓  跑通 + 改参数验证                            │
│  可选第四步：回到项目代码，只读对照，不追求全懂              │
└─────────────────────────────────────────────────────────┘
```

---

## 可选 · 第四步

```
-先在doc下生成"技术学习"目录（如果原来就有这个目录就无需生成直接使用）
-在"技术学习"目录中把上述学习内容整合写在「{技术名称}」_study.md
```

---

## 完整示例：Pydantic Schema（本项目的实际提问）

### 问题一（原文）

> 这个东西在项目中到底有什么不可或缺的作用？假如我不使用它跟使用它会有什么区别？带来的实质好处是什么？按通俗的讲，不要带很多复杂背景知识相关的技能名词。

**收获摘要：** Schema = 接口数据的「登记表 + 自动检查」；不用则每个接口手写 if 检查；用了规则写一次、坏数据门口拦截。

---

### 问题二（原文）

> 我现在初步了解了这个知识，在仍然不了解前置知识的前提下，能不能找到类似的题目让我手操一下？

**收获摘要：** 8 道题，从「联系人登记表」到「迷你备忘录 CRUD」，最后对照 `server/app/schemas/memo.py`。

---

### 问题三（原文）

> 把上面第三题生成一段完整的可执行的代码。

**落地文件：** `xxx/practice_login.py`  
**运行：**

```bash
d:\Cursor\wizzy\server\.venv\Scripts\python.exe d:\Cursor\wizzy\xxx\practice_login.py
```

---

## 复制即用 · 三问合一（懒人版）

```
我想学「{技术名称}」，请按下面三步回答，每一步都要通俗（把我当纯小白，少术语）：

【第一步】它在项目里不可或缺的作用是什么？不用 vs 用了有什么区别？实质好处是什么？
【第二步】给我 5～8 道自包含动手题（由浅入深），并说明练完后对照项目哪个文件。
【第三步】把第 {N} 题写成完整可执行代码，保存到 xxx/practice_{简称}.py，并告诉我怎么运行。

项目背景：{简述项目，如 wizzy 是 FastAPI + Vue 的个人管理工具}
我当前水平：{纯小白 / …}
```

---

## 学习记录表（可自己填）

| 日期 | 技术名称 | 问题一懂了？ | 做到第几题 | 代码跑通？ | 项目对照文件 | 备注 |
|------|----------|--------------|------------|------------|--------------|------|
|      | Pydantic Schema | ✅ | 第 3 题 | ✅ | schemas/memo.py | |
|      |          |              |            |            |              |      |
|      |          |              |            |            |              |      |

---

## 原则（给未来的自己）

1. **先问「为什么」，再问「怎么写」** — 避免只会抄代码不知用途  
2. **一次只学一个概念** — 不要同时问 Schema + Model + ORM  
3. **必须手操** — 看懂 ≠ 会用  
4. **结合本项目** — 抽象例题 + 真实文件对照，记忆最深  
5. **术语后面再补** — 第一遍只要画面感，不要教科书
