# 小智工具箱 — 前端核心技术要点（新手学习指南）

本文档面向前端新手，梳理小智工具箱（`web/`）中涉及的核心技术，说明**是什么、在项目中的作用、代码怎么写**，并配合项目内真实例子。

---

## 目录

1. [Vue 3 + Vite 项目结构](#1-vue-3--vite-项目结构)
2. [Composition API 与 script setup](#2-composition-api-与-script-setup)
3. [Vue Router 与路由守卫](#3-vue-router-与路由守卫)
4. [Pinia 状态管理与持久化](#4-pinia-状态管理与持久化)
5. [Axios 请求封装与拦截器](#5-axios-请求封装与拦截器)
6. [API 模块分层](#6-api-模块分层)
7. [Element Plus UI 组件库](#7-element-plus-ui-组件库)
8. [表单校验（前后端对齐）](#8-表单校验前后端对齐)
9. [布局与公共组件](#9-布局与公共组件)
10. [LocalStorage 本地存储策略](#10-localstorage-本地存储策略)
11. [暗色模式与 UI 状态](#11-暗色模式与-ui-状态)
12. [前端文件导出（TXT）](#12-前端文件导出txt)
13. [组件通信（v-model / defineExpose）](#13-组件通信v-model--defineexpose)
14. [综合示例：从登录到查看密码明文](#14-综合示例从登录到查看密码明文)

---

## 1. Vue 3 + Vite 项目结构

### 是什么

- **Vue 3**：渐进式 JavaScript 框架，本项目使用 Composition API 写法
- **Vite**：新一代前端构建工具，开发时启动快、热更新快

### 在项目中的作用

前端采用「按职责分目录」的组织方式：

```
web/src/
├── api/           # 按业务拆分的 HTTP 请求函数
├── components/    # 可复用组件（PageHeader、CategorySelect 等）
├── layouts/       # 页面布局（MainLayout 侧栏+顶栏）
├── router/        # 路由配置与全局守卫
├── stores/        # Pinia 全局状态（auth、app）
├── utils/         # 工具函数（request、date、storage、export）
├── views/         # 页面级组件（LoginView、MemoListView 等）
├── App.vue        # 根组件（仅 router-view 容器）
└── main.js        # 应用入口，注册插件
```

### 例子：应用入口 main.js

```javascript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import piniaPluginPersistedstate from 'pinia-plugin-persistedstate'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import 'element-plus/theme-chalk/dark/css-vars.css'

const app = createApp(App)
const pinia = createPinia()
pinia.use(piniaPluginPersistedstate)   // Pinia 持久化插件

app.use(pinia)
app.use(router)
app.use(ElementPlus, { locale: zhCn })
app.mount('#app')
```

**启动开发服务器：**

```bash
cd web
npm install
npm run dev
# 访问 http://localhost:5173
```

### 例子：Vite 路径别名与 API 代理

`vite.config.js`：

```javascript
export default defineConfig({
  resolve: {
    alias: { '@': resolve(__dirname, 'src') },  // @/api/auth → src/api/auth
  },
  server: {
    port: 5173,
    proxy: {
      '/api': { target: 'http://127.0.0.1:8080', changeOrigin: true },
    },
  },
})
```

前端请求 `/api/auth/login` 会被代理到后端，避免跨域问题。请确保 `proxy.target` 端口与后端 uvicorn 一致。

---

## 2. Composition API 与 script setup

### 是什么

Vue 3 的 Composition API 用 `ref`、`reactive`、`computed` 等函数组织逻辑，替代 Options API 的 `data`、`methods`。

`<script setup>` 是语法糖，顶层变量和函数自动暴露给模板，代码更简洁。

### 在项目中的作用

所有页面和组件均使用 `<script setup>`，典型模式：

| API | 用途 | 项目中的例子 |
|-----|------|-------------|
| `ref()` | 基本类型响应式 | `loading`、`page`、`memos` |
| `reactive()` | 对象响应式 | 表单 `form`、筛选 `filters` |
| `computed()` | 计算属性 | 弹窗 `visible` 双向绑定 |
| `onMounted()` | 生命周期 | 页面加载时 `loadData()` |
| `watch()` | 监听变化 | CategorySelect 监听 moduleType |

### 例子：登录页状态与提交

`views/LoginView.vue`：

```vue
<script setup>
import { ref, reactive } from 'vue'
import { useRouter } from 'vue-router'
import { useAuthStore } from '@/stores/auth'

const router = useRouter()
const authStore = useAuthStore()
const formRef = ref(null)
const loading = ref(false)

const form = reactive({ username: '', password: '' })

async function handleLogin() {
  await formRef.value.validate()       // Element Plus 表单校验
  loading.value = true
  try {
    await authStore.login(form)        // 调用 Store action
    router.push('/')                   // 跳转首页
  } finally {
    loading.value = false
  }
}
</script>
```

**新手理解：** `ref` 在 script 里要用 `.value` 访问，模板里自动解包；`reactive` 对象直接改属性即可触发视图更新。

---

## 3. Vue Router 与路由守卫

### 是什么

Vue Router 管理 SPA（单页应用）的页面跳转，通过 `router-view` 渲染对应组件，通过 `beforeEach` 守卫在跳转前做鉴权。

### 在项目中的作用

| 路由 | 组件 | 权限 |
|------|------|------|
| `/login` | LoginView | 公开，`meta.public: true` |
| `/memos` | MemoListView | 需登录 |
| `/passwords` | PasswordListView | 需登录 |
| `/todos` | TodoListView | 需登录 |
| `/users` | UserManageView | 需登录 + admin |

### 例子：嵌套路由与懒加载

```javascript
const routes = [
  {
    path: '/login',
    component: () => import('@/views/LoginView.vue'),  // 懒加载，按需打包
    meta: { public: true },
  },
  {
    path: '/',
    component: () => import('@/layouts/MainLayout.vue'),
    redirect: '/memos',
    children: [
      { path: 'memos', component: () => import('@/views/MemoListView.vue') },
      { path: 'users', component: () => import('@/views/UserManageView.vue'), meta: { admin: true } },
    ],
  },
]
```

### 例子：全局路由守卫

```javascript
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()

  // 公开页：已登录访问 /login → 重定向首页
  if (to.meta.public) {
    if (authStore.isLoggedIn && to.path === '/login') next('/')
    else next()
    return
  }

  // 未登录 → 跳转登录页
  if (!authStore.isLoggedIn) { next('/login'); return }

  // 非 admin 访问 admin 路由 → 重定向备忘录
  if (to.meta.admin && !authStore.isAdmin) { next('/memos'); return }

  next()
})
```

**场景演示：**

- 未登录直接访问 `http://localhost:5173/memos` → 自动跳转 `/login`
- 普通用户 `user` 手动访问 `/users` → 重定向到 `/memos`
- 已登录用户访问 `/login` → 重定向到 `/`

---

## 4. Pinia 状态管理与持久化

### 是什么

Pinia 是 Vue 3 官方推荐的状态管理库，替代 Vuex。`pinia-plugin-persistedstate` 可将指定 state 自动同步到 LocalStorage，刷新页面不丢失。

### 在项目中的作用

| Store | 文件 | 管理内容 |
|-------|------|----------|
| `useAuthStore` | `stores/auth.js` | JWT token、用户信息、登录/登出 |
| `useAppStore` | `stores/app.js` | 暗色模式、侧边栏折叠 |

### 例子：authStore 核心逻辑

```javascript
export const useAuthStore = defineStore('auth', {
  state: () => ({
    token: '',
    userInfo: null,
  }),

  getters: {
    isLoggedIn: (state) => !!state.token,
    isAdmin: (state) => state.userInfo?.role === 'admin',
    username: (state) => state.userInfo?.username || '',
  },

  actions: {
    async login(credentials) {
      const res = await loginApi(credentials)
      this.token = res.data.access_token
      this.userInfo = res.data.user
    },
    async logout() {
      if (this.userInfo?.id) clearViewSession(this.userInfo.id)
      this.token = ''
      this.userInfo = null
    },
  },

  persist: {
    key: 'wizzy-auth',
    paths: ['token', 'userInfo'],   // 只持久化这两个字段
  },
})
```

**安全设计：** 密码、查看专用密码等敏感明文**不**存入 Store 或 LocalStorage，只存 token 和非敏感 userInfo。

### 例子：在组件中使用 Store

```javascript
import { useAuthStore } from '@/stores/auth'

const authStore = useAuthStore()

// 读取
authStore.isLoggedIn    // getter
authStore.userInfo.role // state

// 修改
await authStore.login({ username: 'admin', password: 'Admin@123' })
await authStore.logout()
```

MainLayout 中根据 `authStore.isAdmin` 控制「用户管理」菜单是否显示。

---

## 5. Axios 请求封装与拦截器

### 是什么

Axios 是 HTTP 客户端库。本项目在 `utils/request.js` 中创建单例，通过**拦截器**统一处理 Token 附加、业务错误提示、401 自动登出。

### 在项目中的作用

- 所有 `api/*.js` 模块都 import 这个 request 实例
- 页面/Store 不直接操作 Axios，只调 api 函数
- 与后端约定：`code === 0` 为成功，否则弹 ElMessage 错误

### 例子：请求拦截器（自动带 Token）

```javascript
request.interceptors.request.use((config) => {
  const authStore = useAuthStore()
  if (authStore.token) {
    config.headers.Authorization = `Bearer ${authStore.token}`
  }
  return config
})
```

每次请求自动变成：

```http
GET /api/memos
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
```

### 例子：响应拦截器（统一处理业务码）

```javascript
request.interceptors.response.use(
  (response) => {
    if (response.config.responseType === 'blob') return response  // 文件下载跳过 JSON 解析

    const res = response.data
    if (res.code !== 0) {
      ElMessage.error(res.message || '请求失败')
      if (res.code === 401) {
        authStore.logout()
        router.push('/login')
      }
      return Promise.reject(new Error(res.message))
    }
    return res   // 页面拿到的直接是 { code, message, data }
  },
  (error) => {
    ElMessage.error(error.response?.data?.message || '网络错误')
    // ...
  }
)
```

**页面调用示例：**

```javascript
const res = await listMemos({ page: 1, page_size: 10 })
memos.value = res.data.items    // 无需 res.data.data
total.value = res.data.total
```

Token 过期时，用户会看到「令牌已失效」提示并自动跳转登录页。

---

## 6. API 模块分层

### 是什么

将 HTTP 请求按业务拆分到 `src/api/` 目录，每个文件对应后端一个模块，函数名表达业务语义。

### 在项目中的作用

- 页面只关心「调什么接口」，不关心 URL 拼写
- 后端路由变更时，只改 api 文件一处
- 与后端 `app/api/` 目录一一对应

### 例子：认证 API

`api/auth.js`：

```javascript
import request from '@/utils/request'

export function login(data) {
  return request.post('/auth/login', data)
}

export function logout() {
  return request.post('/auth/logout')
}

export function verifyViewPassword(viewPassword) {
  return request.post('/auth/view-password/verify', { view_password: viewPassword })
}
```

### 例子：备忘录 API

`api/memos.js`：

```javascript
export function listMemos(params) {
  return request.get('/memos', { params })
}

export function createMemo(data) {
  return request.post('/memos', data)
}

export function updateMemo(id, data) {
  return request.put(`/memos/${id}`, data)
}

export function deleteMemo(id) {
  return request.delete(`/memos/${id}`)
}
```

**调用链：**

```
MemoListView.vue
  → import { listMemos } from '@/api/memos'
  → listMemos({ page: 1 })
  → request.get('/memos', { params })
  → Vite 代理 → 后端 GET /api/memos
```

---

## 7. Element Plus UI 组件库

### 是什么

Element Plus 是基于 Vue 3 的企业级 UI 组件库，提供表格、表单、对话框、分页等开箱即用组件。

### 在项目中的作用

| 组件 | 使用场景 |
|------|----------|
| `el-table` | 备忘录/密码/待办/用户列表 |
| `el-form` + `el-form-item` | 登录、新建/编辑弹窗 |
| `el-dialog` | 编辑弹窗、修改密码、查看专用密码 |
| `el-pagination` | 列表分页 |
| `el-menu` | MainLayout 侧边栏导航 |
| `el-message` / `el-message-box` | 成功提示、删除确认 |
| `el-switch` | 暗色模式切换 |

### 例子：列表页典型结构

`MemoListView.vue` 模板结构：

```vue
<PageHeader title="备忘录">
  <template #actions>
    <el-button type="primary" @click="openDialog()">新建备忘录</el-button>
  </template>
</PageHeader>

<!-- 搜索区 -->
<el-card>
  <el-form inline>
    <el-form-item label="关键词">
      <el-input v-model="keyword" @keyup.enter="loadData" />
    </el-form-item>
    <el-button type="primary" @click="loadData">搜索</el-button>
  </el-form>
</el-card>

<!-- 数据表格 -->
<el-table :data="memos" v-loading="loading" stripe border>
  <el-table-column prop="title" label="标题" />
  <el-table-column label="操作">
    <template #default="{ row }">
      <el-button link @click="openDialog(row)">编辑</el-button>
      <el-button link type="danger" @click="handleDelete(row)">删除</el-button>
    </template>
  </el-table-column>
</el-table>

<!-- 分页 -->
<el-pagination v-model:current-page="page" :total="total" @change="loadData" />
```

### 例子：删除确认

```javascript
async function handleDelete(row) {
  await ElMessageBox.confirm(`确定删除「${row.title}」？`, '警告', { type: 'warning' })
  await deleteMemo(row.id)
  ElMessage.success('删除成功')
  loadData()
}
```

---

## 8. 表单校验（前后端对齐）

### 是什么

Element Plus 表单通过 `rules` 定义校验规则，在提交前调用 `formRef.validate()` 拦截不合法输入。

### 在项目中的作用

前端 rules 与后端 Pydantic Schema **字段长度、必填规则保持一致**，减少无效请求。

### 例子：登录表单 rules

前端 `LoginView.vue`：

```javascript
const rules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 2, max: 50, message: '用户名长度 2-50 字符', trigger: 'blur' },
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, max: 100, message: '密码长度 6-100 字符', trigger: 'blur' },
  ],
}
```

后端 `schemas/auth.py`：

```python
class LoginRequest(BaseModel):
    username: str = Field(..., min_length=2, max_length=50)
    password: str = Field(..., min_length=6, max_length=100)
```

### 例子：自定义校验器（确认密码）

`ChangePasswordDialog.vue`：

```javascript
const validateConfirm = (rule, value, callback) => {
  if (value !== form.new_password) callback(new Error('两次密码不一致'))
  else callback()
}

const rules = {
  confirm_password: [
    { required: true, message: '请确认新密码', trigger: 'blur' },
    { validator: validateConfirm, trigger: 'blur' },
  ],
}
```

---

## 9. 布局与公共组件

### 是什么

- **Layout**：定义页面骨架（侧栏 + 顶栏 + 内容区），子路由在 `<router-view>` 中切换
- **公共组件**：多处复用的 UI 片段，通过 props / slots / emit 与父组件通信

### 在项目中的作用

| 文件 | 作用 |
|------|------|
| `layouts/MainLayout.vue` | 侧栏菜单、顶栏设置、暗色切换、`<router-view>` |
| `components/PageHeader.vue` | 统一页面标题 + 操作按钮插槽 |
| `components/CategorySelect.vue` | 分类下拉，可选增删改 |
| `components/ChangePasswordDialog.vue` | 修改密码弹窗 |
| `components/ViewPasswordDialog.vue` | 设置查看专用密码 |

### 例子：PageHeader 插槽

`components/PageHeader.vue`：

```vue
<template>
  <div class="page-header">
    <h3 class="title">{{ title }}</h3>
    <div class="actions">
      <slot name="actions" />   <!-- 父组件注入按钮 -->
    </div>
  </div>
</template>
```

使用：

```vue
<PageHeader title="备忘录">
  <template #actions>
    <el-button @click="handleExportAll">导出全部 TXT</el-button>
    <el-button type="primary" @click="openDialog()">新建</el-button>
  </template>
</PageHeader>
```

### 例子：MainLayout 权限菜单

```vue
<el-sub-menu index="manage" v-if="authStore.isAdmin">
  <template #title><el-icon><User /></el-icon><span>用户管理</span></template>
  <el-menu-item index="/users">用户列表</el-menu-item>
</el-sub-menu>
```

普通用户登录后，「用户管理」菜单不会出现（与路由守卫双重保护）。

---

## 10. LocalStorage 本地存储策略

### 是什么

浏览器 LocalStorage 可持久化键值对。本项目**有选择地**使用它，而非把所有数据都存本地。

### 在项目中的作用

| 存储内容 | 工具函数 | 说明 |
|----------|----------|------|
| JWT + userInfo | Pinia persist | 刷新页面保持登录 |
| 暗色模式/侧栏状态 | Pinia persist | UI 偏好 |
| 备忘录草稿 | `saveMemoDraft()` | 新建时防丢失，key 含 userId |
| 查看密码会话 | `saveViewSession()` | 24h 内免重复输入查看密码 |

**不存储：** 登录密码、密码本明文、AES 密钥。

### 例子：备忘录草稿（防抖保存）

`utils/storage.js`：

```javascript
const DRAFT_PREFIX = 'wizzy_memo_draft_'

export function saveMemoDraft(userId, draft) {
  localStorage.setItem(`${DRAFT_PREFIX}${userId}`, JSON.stringify({ ...draft, savedAt: Date.now() }))
}

export function loadMemoDraft(userId) {
  const raw = localStorage.getItem(`${DRAFT_PREFIX}${userId}`)
  return raw ? JSON.parse(raw) : null
}
```

`MemoListView.vue` 中使用：

```javascript
// 输入时防抖 500ms 自动保存
function saveDraftDebounced() {
  if (editId.value) return   // 编辑模式不存草稿
  clearTimeout(draftTimer)
  draftTimer = setTimeout(() => {
    saveMemoDraft(authStore.userInfo.id, { title: form.title, content: form.content, category_id: form.category_id })
  }, 500)
}

// 打开新建弹窗时恢复草稿
function openDialog(row) {
  if (!row) {
    const draft = loadMemoDraft(authStore.userInfo.id)
    if (draft) Object.assign(form, draft)
  }
}

// 保存成功后清除草稿
await createMemo(form)
clearMemoDraft(authStore.userInfo.id)
```

**key 含 userId** 的原因：admin 和 user 切换登录时，草稿不会互相污染。

### 例子：查看密码会话

```javascript
// 验证查看专用密码成功后
saveViewSession(userId, viewSessionToken)

// 后续 reveal 请求优先带 view_session
const viewSession = getViewSession(userId)
const payload = viewSession
  ? { view_session: viewSession }
  : { view_password: verifyPassword.value }
await revealPassword(row.id, payload)

// 登出 / 改密 / 修改查看密码时清除
clearViewSession(userId)
```

---

## 11. 暗色模式与 UI 状态

### 是什么

Element Plus 内置暗色主题，通过在 `<html>` 上切换 `dark` class 实现。状态保存在 `appStore` 并持久化。

### 在项目中的作用

- 顶栏 `el-switch` 切换亮/暗
- 刷新页面后保持用户选择
- 侧边栏折叠宽度也在 appStore 中管理

### 例子：切换暗色模式

`stores/app.js`：

```javascript
toggleDarkMode() {
  this.darkMode = !this.darkMode
  if (this.darkMode) {
    document.documentElement.classList.add('dark')
  } else {
    document.documentElement.classList.remove('dark')
  }
}
```

`main.js` 启动时恢复：

```javascript
const appStore = useAppStore()
if (appStore.darkMode) {
  document.documentElement.classList.add('dark')
}
```

MainLayout 顶栏绑定：

```vue
<el-switch v-model="darkMode" active-text="暗" inactive-text="亮" @change="onDarkChange" />
```

---

## 12. 前端文件导出（TXT）

### 是什么

不依赖后端，在前端用 `Blob` + 临时 `<a>` 标签触发浏览器下载。

### 在项目中的作用

备忘录支持单条/批量导出 TXT，UTF-8 BOM 确保 Windows 记事本正确显示中文。

### 例子：export.js 实现

```javascript
function downloadTxt(filename, content) {
  const blob = new Blob(['\uFEFF' + content], { type: 'text/plain;charset=utf-8' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = filename
  a.click()
  URL.revokeObjectURL(url)   // 释放内存
}

export function exportMemoTxt(memo, getCategoryName) {
  const text = [
    `标题：${memo.title}`,
    `分类：${getCategoryName(memo.category_id)}`,
    `内容：`, memo.content,
  ].join('\n')
  downloadTxt(`memo_${memo.id}.txt`, text)
}
```

`MemoListView.vue` 调用：

```javascript
function handleExportOne(row) {
  exportMemoTxt(row, getCategoryName)
}
```

密码本备份则走后端 API，下载 JSON 文件（含加密字段 `password_enc`）。

---

## 13. 组件通信（v-model / defineExpose）

### 是什么

Vue 3 组件间通信常用方式：

- **props / emit**：父 → 子、子 → 父
- **v-model**：`:modelValue` + `@update:modelValue` 语法糖
- **defineExpose**：子组件暴露方法给父组件 ref 调用

### 在项目中的作用

`CategorySelect` 是典型可复用组件：备忘录、密码本、待办三个页面都用它选分类。

### 例子：CategorySelect 的 v-model

子组件：

```vue
<el-select
  :model-value="modelValue"
  @update:model-value="$emit('update:modelValue', $event)"
>
  <el-option v-for="cat in categories" :key="cat.id" :label="cat.name" :value="cat.id" />
</el-select>
```

父组件：

```vue
<CategorySelect v-model="categoryId" module-type="memo" manageable @updated="onCategoryUpdated" />
```

等价于：

```vue
<CategorySelect
  :model-value="categoryId"
  @update:model-value="categoryId = $event"
  module-type="memo"
/>
```

### 例子：defineExpose 暴露 refresh

```javascript
// CategorySelect.vue
defineExpose({ refresh: loadCategories })

// 父组件
const filterCategorySelectRef = ref(null)
async function onCategoryUpdated() {
  await loadCategories()
  filterCategorySelectRef.value?.refresh()   // 刷新下拉选项
}
```

### 例子：弹窗 v-model（computed 双向绑定）

```javascript
const props = defineProps({ modelValue: Boolean })
const emit = defineEmits(['update:modelValue'])

const visible = computed({
  get: () => props.modelValue,
  set: (v) => emit('update:modelValue', v),
})
```

父组件：`<ChangePasswordDialog v-model="showChangePassword" />`

---

## 14. 综合示例：从登录到查看密码明文

以下串联上述技术点，演示前端完整交互流程。

### 步骤 1：用户登录

**用户操作：** 在 LoginView 输入 admin / Admin@123，点击登录

**代码路径：**

```
LoginView.handleLogin()
  → formRef.validate()                    // Element Plus 表单校验
  → authStore.login(form)
      → api/auth.login(data)              // Axios POST /api/auth/login
      → request 拦截器附加（此时无 token）
      → 响应拦截器检查 code === 0
      → this.token = res.data.access_token
      → this.userInfo = res.data.user
      → Pinia persist 写入 LocalStorage
  → router.push('/')
  → 路由守卫：isLoggedIn=true，放行
  → MainLayout + MemoListView 渲染
```

### 步骤 2：加载密码列表

**用户操作：** 点击侧栏「密码本」

**代码路径：**

```
router → PasswordListView
  → onMounted: loadData() + loadViewPasswordStatus()
  → api/passwords.listPasswords({ page: 1 })
      → request 拦截器：Authorization: Bearer <token>
  → passwords.value = res.data.items
  → 表格显示 password_masked: "G****3"
```

### 步骤 3：设置查看专用密码

**用户操作：** 点击「查看专用密码」，填写登录密码和查看密码

```
ViewPasswordDialog.handleSubmit()
  → setViewPassword({ login_password, view_password })
  → clearViewSession(userId)    // 修改后旧会话失效
  → emit('saved')
```

### 步骤 4：点击查看明文

**用户操作：** 点击表格中脱敏密码单元格

```
PasswordListView.handlePasswordClick(row)
  → getViewSession(userId) 为空？
      → 弹出 verifyVisible 对话框，输入查看专用密码
      → verifyViewPassword(pwd)
      → saveViewSession(userId, res.data.view_session)
  → revealWithSession(row)
      → revealPassword(row.id, { view_session })
  → revealedMap[row.id] = plain   // 单元格显示明文
  → 再次点击 → delete revealedMap[row.id]  // 隐藏
```

### 步骤 5：401 自动登出

**场景：** Token 被后端 revoke（改密、管理员禁用）

```
任意 API 返回 { code: 401, message: "令牌已失效" }
  → request 响应拦截器
  → ElMessage.error("令牌已失效")
  → authStore.logout()
  → router.push('/login')
  → Pinia persist 清除 token
```

---

## 学习路径建议

1. **跑起来**：`cd web && npm run dev`，浏览器 F12 观察 Network 面板
2. **跟登录链路**：`LoginView` → `authStore` → `api/auth` → `request.js`
3. **跟列表页**：选一个 `MemoListView`，理解「加载 → 表格 → 弹窗 CRUD → 分页」模式
4. **理解守卫与 Store**：刷新页面为什么还保持登录？看 Pinia persist
5. **对比密码本**：理解 view_session 与 LocalStorage 的配合
6. **读测试文档**：[doc/开发环境测试说明.md](doc/开发环境测试说明.md) 按用例验证

## 相关文档

- [web/readme.md](web/readme.md) — 前端目录与开发命令
- [后端核心技术要点.md](后端核心技术要点.md) — 后端技术对照学习
- [doc/后端技术架构.md](doc/后端技术架构.md) — 前后端协作架构
- [readme.md](readme.md) — 项目总览与快速启动
