# 小智工具箱 — 后端核心技术要点（新手学习指南）

本文档面向后端新手，梳理小智工具箱（`server/`）中涉及的核心技术，说明**是什么、在项目中的作用、代码怎么写**，并配合项目内真实例子。

---

## 目录

1. [FastAPI 与三层架构](#1-fastapi-与三层架构)
2. [依赖注入（Depends）](#2-依赖注入depends)
3. [Pydantic Schema 请求校验](#3-pydantic-schema-请求校验)
4. [SQLAlchemy 2.x 异步 ORM](#4-sqlalchemy-2x-异步-orm)
5. [统一响应格式与业务异常](#5-统一响应格式与业务异常)
6. [JWT 鉴权 + Token 白名单](#6-jwt-鉴权--token-白名单)
7. [Bcrypt 密码哈希](#7-bcrypt-密码哈希)
8. [AES/Fernet 对称加密](#8-aesfernet-对称加密)
9. [行级数据隔离](#9-行级数据隔离)
10. [操作日志审计](#10-操作日志审计)
11. [配置管理（pydantic-settings）](#11-配置管理pydantic-settings)
12. [综合示例：从登录到查看密码明文](#12-综合示例从登录到查看密码明文)

---

## 1. FastAPI 与三层架构

### 是什么

FastAPI 是基于 Python 的现代 Web 框架，原生支持 `async/await`、自动生成 OpenAPI 文档（Swagger）。

本项目采用经典**三层架构**：

```
HTTP 请求
    ↓
API 路由层   app/api/       接收 HTTP、参数绑定、返回 JSON
    ↓
业务服务层   app/services/  业务逻辑、权限、加密、日志
    ↓
数据访问层   app/models/    ORM 模型，映射 MySQL 表
    ↓
MySQL 数据库
```

### 在项目中的作用

- **路由层**只做「接线员」：解析请求、调用 service、包装响应，不写复杂 SQL
- **服务层**是「大脑」：CRUD、鉴权校验、加密解密、记日志都在这里
- **模型层**是「表结构定义」：字段类型、外键关系、索引

分层后，同一套 service 可被多个 API 复用，测试和维护都更简单。

### 例子：创建备忘录的完整调用链

**路由层** `app/api/memos.py`：

```python
@router.post("")
async def create_memo(
    req: MemoCreateRequest,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    memo = await memo_service.create_memo(db, current_user, req)
    return success(MemoResponse.model_validate(memo).model_dump(), "创建成功")
```

**服务层** `app/services/memo_service.py`：

```python
async def create_memo(db: AsyncSession, user: User, req: MemoCreateRequest) -> Memo:
    memo = Memo(
        user_id=user.id,
        title=req.title,
        content=req.content,
        category_id=req.category_id,
        is_pinned=req.is_pinned,
    )
    db.add(memo)
    await db.flush()
    return memo
```

**模型层** `app/models/memo.py`：

```python
class Memo(Base):
    __tablename__ = "memos"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id", ondelete="CASCADE"))
    title: Mapped[str] = mapped_column(String(200), nullable=False)
    content: Mapped[str] = mapped_column(Text, default="", nullable=False)
```

**请求示例：**

```http
POST /api/memos
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
Content-Type: application/json

{
  "title": "购物清单",
  "content": "牛奶、面包、鸡蛋",
  "category_id": 1,
  "is_pinned": false
}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "创建成功",
  "data": {
    "id": 4,
    "title": "购物清单",
    "content": "牛奶、面包、鸡蛋",
    "is_pinned": false,
    "created_at": "2026-06-24T10:00:00"
  }
}
```

---

## 2. 依赖注入（Depends）

### 是什么

依赖注入（Dependency Injection）是一种设计模式：**把公共准备工作（拿数据库连接、解析 Token、校验权限）抽成独立函数，路由通过 `Depends(...)` 声明需要它们，FastAPI 自动按顺序调用。**

### 在项目中的作用

| 依赖函数 | 文件 | 作用 |
|----------|------|------|
| `get_db` | `core/database.py` | 每个请求开一个 DB 会话，结束自动 commit/rollback |
| `get_current_user` | `core/deps.py` | 从 Header 解析 JWT，校验 Token 白名单，返回当前用户 |
| `require_admin` | `core/deps.py` | 在已登录基础上，额外要求 `role == admin` |
| `get_client_ip` | `core/deps.py` | 读取客户端 IP，供操作日志使用 |

### 例子：数据库会话自动管理

`core/database.py`：

```python
async def get_db():
    async with AsyncSessionLocal() as session:
        try:
            yield session
            await session.commit()      # 正常结束 → 提交事务
        except Exception:
            await session.rollback()    # 异常 → 回滚
            raise
```

路由中只需声明：

```python
async def list_memos(db: AsyncSession = Depends(get_db), ...):
    ...
```

FastAPI 保证：**一个 HTTP 请求对应一个 session，请求结束 session 自动关闭。**

### 例子：管理员接口权限控制

`app/api/users.py` 中创建用户接口：

```python
@router.post("")
async def create_user(
    req: UserCreateRequest,
    db: AsyncSession = Depends(get_db),
    admin: User = Depends(require_admin),   # 非 admin 直接 403
    ip: str = Depends(get_client_ip),
):
    user = await user_service.create_user(db, admin, req, ip)
    return success(UserResponse.model_validate(user).model_dump(), "创建成功")
```

普通用户 `user` 调用 `POST /api/users` 时，`require_admin` 会抛出：

```json
{ "code": 403, "message": "权限不足，需要管理员角色", "data": null }
```

---

## 3. Pydantic Schema 请求校验

### 是什么

Pydantic 是基于 Python 类型注解的数据校验库。FastAPI 用它定义「请求体长什么样、字段有什么约束」，不合法的数据在进入业务逻辑前就被拦截。

### 在项目中的作用

- **入参校验**：用户名长度、密码长度、枚举值等
- **出参建模**：ORM 对象 → 响应 DTO，避免泄露 `password_hash` 等敏感字段
- **前后端对齐**：前端 Element Plus 表单 rules 与后端 Schema 规则一致

### 例子：登录请求校验

`app/schemas/auth.py`：

```python
class LoginRequest(BaseModel):
    username: str = Field(..., min_length=2, max_length=50)
    password: str = Field(..., min_length=6, max_length=100)
```

**合法请求：**

```json
{ "username": "admin", "password": "Admin@123" }
```

**非法请求（密码太短）：**

```json
{ "username": "admin", "password": "123" }
```

FastAPI 直接返回 HTTP 422，不会进入 `auth_service.login()`。

### 例子：ORM 转响应（脱敏）

`UserInfoResponse` 只暴露安全字段：

```python
class UserInfoResponse(BaseModel):
    id: int
    username: str
    role: str
    is_active: bool
    has_view_password: bool = False
    model_config = {"from_attributes": True}
```

使用方式：

```python
auth_service.to_user_info(user).model_dump()
# 返回 {"id": 1, "username": "admin", "role": "admin", ...}
# 不包含 password_hash
```

---

## 4. SQLAlchemy 2.x 异步 ORM

### 是什么

SQLAlchemy 是 Python 最流行的 ORM（对象关系映射）库。本项目使用 **2.x 异步模式**，配合 `aiomysql` 驱动，与 FastAPI 的 `async/await` 风格一致。

### 在项目中的作用

- 用 Python 类描述数据库表（`app/models/`）
- 用 `select()` 构建查询，避免手写 SQL 字符串
- 通过外键、`ondelete="CASCADE"` 维护表间关系

### 例子：分页查询备忘录

`app/services/memo_service.py`：

```python
async def list_memos(db, user, keyword=None, category_id=None, page=1, page_size=10):
    # 基础条件：只看自己的数据
    query = select(Memo).where(Memo.user_id == user.id)
    count_query = select(func.count(Memo.id)).where(Memo.user_id == user.id)

    # 关键词搜索
    if keyword:
        kw_filter = or_(Memo.title.contains(keyword), Memo.content.contains(keyword))
        query = query.where(kw_filter)
        count_query = count_query.where(kw_filter)

    # 分页
    total = (await db.execute(count_query)).scalar() or 0
    query = query.order_by(Memo.is_pinned.desc(), Memo.updated_at.desc())
    query = query.offset((page - 1) * page_size).limit(page_size)
    result = await db.execute(query)
    return list(result.scalars().all()), total
```

**对应 SQL 逻辑（简化）：**

```sql
SELECT COUNT(*) FROM memos WHERE user_id = 1 AND title LIKE '%欢迎%';
SELECT * FROM memos WHERE user_id = 1 AND title LIKE '%欢迎%'
ORDER BY is_pinned DESC, updated_at DESC
LIMIT 10 OFFSET 0;
```

**新手注意：** 所有数据库操作必须 `await`，否则会阻塞事件循环，导致并发性能下降。

---

## 5. 统一响应格式与业务异常

### 是什么

项目所有 API 返回统一 JSON 结构 `{ code, message, data }`。业务错误通过自定义 `BusinessException` 抛出，由全局异常处理器转换为标准响应。

### 在项目中的作用

- 前端只需判断 `code === 0` 即成功，无需解析多种 HTTP 状态码
- 业务层用 `raise BusinessException(...)` 表达「用户不存在」「权限不足」等，代码清晰
- 未捕获的系统异常统一返回 500，避免泄露堆栈给前端

### 例子：成功与失败响应

**成功：**

```python
return success({"items": [...], "total": 10})
# → { "code": 0, "message": "success", "data": { "items": [...], "total": 10 } }
```

**业务失败（备忘录不存在）：**

```python
raise BusinessException("备忘录不存在", code=404)
# → HTTP 200, body: { "code": 404, "message": "备忘录不存在", "data": null }
```

**全局处理器** `core/exceptions.py`：

```python
@app.exception_handler(BusinessException)
async def business_exception_handler(request, exc):
    return JSONResponse(
        status_code=200,
        content={"code": exc.code, "message": exc.message, "data": None},
    )
```

---

## 6. JWT 鉴权 + Token 白名单

### 是什么

JWT（JSON Web Token）是一种无状态的令牌格式，服务端签发、客户端保存、每次请求携带。

**纯 JWT 的问题：** 签发后无法主动作废（只能等过期）。

**本项目的方案：** JWT + `user_tokens` 表做白名单，每个 Token 有唯一 `jti`，登出/改密/禁用时将 `jti` 标记为 `is_revoked=True`。

### 在项目中的作用

| 场景 | 处理方式 |
|------|----------|
| 登录 | 签发 JWT，写入 `user_tokens` |
| 每次请求 | 解析 JWT + 查表确认 jti 未 revoke |
| 登出 | revoke 当前 jti |
| 改密码 | revoke 该用户全部 jti（强制所有设备下线） |
| 禁用用户 | `is_active=False`，下次请求拒绝 |

### 例子：JWT Payload 结构

`core/security.py` 签发时：

```python
payload = {
    "sub": str(user_id),       # 用户 ID
    "username": username,
    "role": role,              # admin / user
    "jti": str(uuid.uuid4()),  # 唯一令牌 ID
    "exp": expire,             # 过期时间
}
token = jwt.encode(payload, settings.JWT_SECRET, algorithm="HS256")
```

### 例子：请求校验流程

`core/deps.py` 中 `get_current_user`：

```python
# 1. 从 Header 取 Bearer Token
token = authorization[7:]
payload = decode_access_token(token)

# 2. 查 user_tokens 白名单
result = await db.execute(
    select(UserToken).where(
        UserToken.jti == jti,
        UserToken.user_id == user_id,
        UserToken.is_revoked == False,
    )
)
if not token_record:
    raise BusinessException("令牌已失效，请重新登录", code=401)

# 3. 查用户是否被禁用
if not user or not user.is_active:
    raise BusinessException("用户不存在或已被禁用", code=401)
```

**改密码后强制下线** `auth_service.py`：

```python
user.password_hash = hash_password(req.new_password)
await db.execute(
    update(UserToken).where(UserToken.user_id == user.id).values(is_revoked=True)
)
```

---

## 7. Bcrypt 密码哈希

### 是什么

Bcrypt 是一种**单向**密码哈希算法，内置随机 salt，同一明文每次 hash 结果不同，无法从 hash 反推明文。

### 在项目中的作用

用于存储**不需要还原**的密码：

| 密码类型 | 存储字段 | 说明 |
|----------|----------|------|
| 登录密码 | `users.password_hash` | 登录时 verify，不存明文 |
| 查看专用密码 | `users.view_password_hash` | 查看密码本明文前的二次验证 |

### 例子：登录验证

`core/security.py`：

```python
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(plain_password: str) -> str:
    return pwd_context.hash(plain_password)

def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)
```

`auth_service.login()` 中：

```python
if not user or not verify_password(req.password, user.password_hash):
    raise BusinessException("用户名或密码错误", code=401)
```

**种子数据写入** `scripts/seed_data.py`：

```python
admin = User(username="admin", password_hash=hash_password("Admin@123"), role="admin")
```

数据库中存的是类似 `$2b$12$xxxx...` 的 hash，不是 `Admin@123` 明文。

---

## 8. AES/Fernet 对称加密

### 是什么

AES 是对称加密算法，**加密后可解密**。本项目使用 `cryptography` 库的 Fernet 封装，密钥来自 `.env` 的 `AES_KEY`。

### 在项目中的作用

密码本条目需要**展示和复制明文**，不能用 Bcrypt（不可逆），因此：

- 入库：`aes_encrypt(明文)` → 存 `password_enc` 字段
- 查看：`aes_decrypt(password_enc)` → 返回明文
- 列表：只返回脱敏 `password_masked`，不返回密文

### 例子：创建密码条目

`password_service.create_password()`：

```python
entry = PasswordEntry(
    user_id=user.id,
    site_name=req.site_name,
    username=req.username,
    password_enc=aes_encrypt(req.password),  # "GitHubPass123" → 密文入库
    url=req.url,
)
db.add(entry)
return to_password_response(entry)  # 列表只返回 "G****3"
```

### 例子：查看明文（二次验证）

`password_service.reveal_password()`：

```python
# 1. 验证查看专用密码 或 24h view_session
if req.view_session:
    session_user_id = decode_view_session_token(req.view_session)
    verified = (session_user_id == user.id)
if not verified:
    verify_password(req.view_password, user.view_password_hash)

# 2. 解密并记审计日志
entry = await get_password_entry(db, user, entry_id)
plain = aes_decrypt(entry.password_enc)
await log_operation(db, user, "view_password", "password", f"查看密码 {entry.site_name}", ip)
return plain
```

**脱敏规则** `core/security.py`：

```python
def mask_password(password: str) -> str:
    if len(password) <= 2:
        return "****"
    return password[0] + "****" + password[-1]
# "GitHubPass123" → "G****3"
```

---

## 9. 行级数据隔离

### 是什么

多用户共用同一套业务表（memos、password_entries、todos），通过每条记录的 `user_id` 字段区分归属。Service 层**强制**在每次查询中加 `WHERE user_id = current_user.id`。

### 在项目中的作用

- admin 和 user 各自只能看到自己的备忘录、密码、待办
- 即使猜到别人的记录 ID，也无法访问（返回 404，不暴露「该 ID 是否存在」）
- 不依赖数据库行级安全（RLS），在应用层实现，逻辑清晰

### 例子：获取单条备忘录

`memo_service.get_memo()`：

```python
result = await db.execute(
    select(Memo).where(Memo.id == memo_id, Memo.user_id == user.id)
)
memo = result.scalar_one_or_none()
if not memo:
    raise BusinessException("备忘录不存在", code=404)
```

**场景：** user（id=2）尝试访问 admin（id=1）的 memo_id=3：

```http
GET /api/memos/3
Authorization: Bearer <user的token>
```

→ 查询条件 `id=3 AND user_id=2` 无结果 → 返回 404，不会泄露 admin 的数据。

### 例子：创建时绑定 user_id

```python
memo = Memo(user_id=user.id, title=req.title, ...)
```

新建数据自动归属当前登录用户，前端无需传 `user_id`。

---

## 10. 操作日志审计

### 是什么

将高危操作（登录、删密码、导出备份、改密、禁用用户等）写入 `operation_logs` 表，记录操作人、动作、模块、详情、IP、时间。

### 在项目中的作用

- **安全审计**：谁在什么时候查看了密码明文、导出了备份
- **问题排查**：用户反馈「账号异常」时可追溯
- **权限分级**：admin 可查全部日志，普通用户只能查自己的

### 例子：写入日志

`operation_log_service.log_operation()`：

```python
log = OperationLog(
    user_id=user.id,
    action=action,      # 如 "login", "delete", "view_password"
    module=module,      # 如 "auth", "password"
    detail=detail,      # 如 "用户 admin 登录"
    ip=ip,
)
db.add(log)
await db.flush()
```

**登录时调用** `auth_service.login()`：

```python
await log_operation(db, user, "login", "auth", f"用户 {user.username} 登录", ip)
```

**删除密码时调用** `password_service.delete_password()`：

```python
await log_operation(db, user, "delete", "password", f"删除密码条目 {entry.site_name}", ip)
```

### 例子：查询日志（权限过滤）

```python
if user.role != "admin":
    query = query.where(OperationLog.user_id == user.id)
```

---

## 11. 配置管理（pydantic-settings）

### 是什么

`pydantic-settings` 从 `.env` 文件和环境变量加载配置，自动做类型转换和校验，生成全局 `settings` 单例。

### 在项目中的作用

- 开发/生产环境切换只需改 `.env`，代码不动
- 敏感信息（数据库密码、JWT 密钥、AES 密钥）不硬编码在源码里
- 类型安全：`JWT_EXPIRE_MINUTES` 自动转为 `int`

### 例子：配置类

`core/config.py`：

```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")

    DATABASE_URL: str = "mysql+aiomysql://wizzy:wizzy123@127.0.0.1:3306/wizzy_db"
    JWT_SECRET: str = "change-me-in-production"
    JWT_EXPIRE_MINUTES: int = 10080   # 7 天
    AES_KEY: str = ""
    CORS_ORIGINS: str = "http://localhost:5173"
    DEBUG: bool = True

settings = Settings()
```

**`.env` 示例：**

```ini
DATABASE_URL=mysql+aiomysql://root:你的密码@127.0.0.1:3306/wizzy_db
JWT_SECRET=dev-secret-key
AES_KEY=你的Fernet密钥
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
DEBUG=true
```

**生成 AES 密钥：**

```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

---

## 12. 综合示例：从登录到查看密码明文

以下串联上述所有技术点，演示一次完整业务流程。

### 步骤 1：登录

```http
POST /api/auth/login
Content-Type: application/json

{ "username": "admin", "password": "Admin@123" }
```

| 技术点 | 在此步骤的作用 |
|--------|----------------|
| Pydantic | `LoginRequest` 校验 username/password 长度 |
| Bcrypt | `verify_password()` 比对哈希 |
| JWT | `create_access_token()` 签发含 jti 的 Token |
| Token 白名单 | jti 写入 `user_tokens` 表 |
| 操作日志 | `log_operation("login", ...)` |
| 统一响应 | `success(LoginResponse(...))` |

**响应：**

```json
{
  "code": 0,
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "user": { "id": 1, "username": "admin", "role": "admin", "has_view_password": false }
  }
}
```

### 步骤 2：查询密码列表（脱敏）

```http
GET /api/passwords?page=1
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
```

| 技术点 | 在此步骤的作用 |
|--------|----------------|
| 依赖注入 | `get_current_user` 解析 Token、查白名单 |
| 行级隔离 | `WHERE user_id = 1` |
| AES | 不解密，只返回 `password_masked: "G****3"` |

### 步骤 3：设置查看专用密码

```http
POST /api/auth/view-password
Authorization: Bearer ...
{ "login_password": "Admin@123", "view_password": "View@456" }
```

| 技术点 | 在此步骤的作用 |
|--------|----------------|
| Bcrypt | 验证登录密码 + hash 存储查看专用密码 |
| 操作日志 | 记录 `set_view_password` |

### 步骤 4：验证查看密码，获取 24h 会话

```http
POST /api/auth/view-password/verify
{ "view_password": "View@456" }
```

**响应：**

```json
{ "code": 0, "data": { "view_session": "eyJ...", "expires_in_hours": 24 } }
```

| 技术点 | 在此步骤的作用 |
|--------|----------------|
| Bcrypt | 验证查看专用密码 |
| JWT | 签发 `type=view_session` 的短期 Token |

### 步骤 5：Reveal 密码明文

```http
POST /api/passwords/1/reveal
Authorization: Bearer ...
{ "view_session": "eyJ..." }
```

| 技术点 | 在此步骤的作用 |
|--------|----------------|
| JWT | `decode_view_session_token()` 验证会话 |
| 行级隔离 | `get_password_entry()` 带 user_id 条件 |
| AES | `aes_decrypt(entry.password_enc)` |
| 操作日志 | 记录 `view_password` 审计 |

**响应：**

```json
{ "code": 0, "data": { "password": "GitHubPass123" } }
```

---

## 学习路径建议

1. **跑起来**：`cd server && uvicorn main:app --reload --port 8000`，打开 http://127.0.0.1:8000/docs
2. **跟登录链路**：`api/auth.py` → `auth_service.py` → `security.py` → `models/user.py`
3. **跟 CRUD 链路**：`api/memos.py` → `memo_service.py` → `models/memo.py`
4. **理解安全差异**：Bcrypt（不可逆，用于登录） vs AES（可逆，用于密码本）
5. **读测试文档**：[doc/开发环境测试说明.md](doc/开发环境测试说明.md) 按用例逐功能验证

## 相关文档

- [server/readme.md](server/readme.md) — 后端目录与 API 模块概览
- [doc/后端技术架构.md](doc/后端技术架构.md) — 架构设计
- [doc/后端程序流程.md](doc/后端程序流程.md) — 请求处理流程
- [doc/数据库说明.md](doc/数据库说明.md) — 表结构与索引
